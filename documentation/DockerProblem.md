# ------------------------------------
# Unfamiliar Error Docker Problem (IMAGE CONFLICTS)
# ------------------------------------

Scenario:
````
v5 (Older Version) - network configs deleted (bugged image), keeps displaying even when the newer version was composed

v8 (Latest Version works on personal computer) - updated net configs, but  when composed with the same configs, network error problem occurs upon integrating to another pc
```



# Things To Do:

```
- Since it's dedicated only for 'local access maybe we need to add to environmental variables inside windows

- Reviewed the docker configs, configs are the same, routing all the same, just added gmail input text field in superadmin page for OTP

- Admin Bypass also don't work, I created a fixed password (for now) to test the admin bypass for debugging and testing  purposes

- npm live server works, but routing 127.0.0.1 (same as before) don't.

```